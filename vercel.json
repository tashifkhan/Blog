{
	"buildCommand": "bun run build",
	"installCommand": "bun install",
	"framework": "astro",
	"functions": {
		"src/pages/api/**/*.ts": {
			"runtime": "edge"
		}
	},
	"routes": [
		{
			"handle": "filesystem"
		},
		{
			"src": "/(.*)",
			"status": 404,
			"dest": "https://tashif.codes"
		}
	]
}
